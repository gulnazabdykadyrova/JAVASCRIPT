// Given a array of integers as an array, determine whether it is possible to obtain a strictly increasing array by removing no more than one element from the array.

// Example

// For array = [1, 3, 2, 1], the output should be almostIncreasingarray(array) = false.

// There is no one element in this array that can be removed in order to get a strictly increasing array.

// For array = [1, 3, 2], the output should be almostIncreasingarray(array) = true.

// You can remove 3 from the array to get the strictly increasing array [1, 2]. Alternately, you can remove 2 to get the strictly increasing array [1, 3].

// Input/Output

// [execution time limit] 4 seconds (js)

// [input] array.integer array

// Guaranteed constraints: 2 ≤ array.length ≤ 105, -105 ≤ array[i] ≤ 105.

// [output] boolean

// Return true if it is possible to remove one element from the array in order to get a strictly increasing array, otherwise return false.

function almostIncreasingSequence(sequence) {}

console.log(almostIncreasingSequence([1, 3, 2, 1]));
